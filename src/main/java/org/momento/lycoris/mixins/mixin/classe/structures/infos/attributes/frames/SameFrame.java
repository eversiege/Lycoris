package org.momento.lycoris.mixins.mixin.classe.structures.infos.attributes.frames;

public class SameFrame extends StackMapFrame {
    public SameFrame(Type type) {
        super(type);
    }
}
